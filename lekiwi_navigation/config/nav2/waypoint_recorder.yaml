waypoint_recorder_node:
  ros__parameters:
    number_of_loops: 4294967295  # uint32 max - effectively continuous patrol; 0 = single pass
    frame_id: map
    marker_topic: /waypoint_markers
    max_retries: 3  # consecutive failures at the same waypoint before dropping it
